MMGDINOB_PATH = 'your_checkpoint_path/grounding_dino_swin-b_pretrain_all-f9818a7c.pth'

# Only needed if you're retraining from scratch on your own annotations,
# not for running inference with the released weights/model.pth checkpoint.
JOINT_URINEBAG_PATH = 'your_dataset_path/joint_urinebag_dataset'

# Only needed if you're retraining the apple/tomato fruits model, not for
# running inference with the released weights/model_fruits.pth checkpoint.
FRUITS_DATASET_PATH = 'your_dataset_path/fruits_dataset'